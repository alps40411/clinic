# LIFF 配置
# 是否启用 Mock 模式（开发环境建议设为 true）
VITE_LIFF_MOCK=true

# Mock 模式下使用的测试用户 ID
# 在 Mock 模式下，API 将使用此 ID 作为 x-line-id header
VITE_MOCK_USER_ID=U66bfb7dabdef424cd78c29bd352fc4cb

# 生产环境配置示例：
# VITE_LIFF_MOCK=false
# 生产环境不需要设置 VITE_MOCK_USER_ID，因为会从真实的 LIFF 获取用户 ID
